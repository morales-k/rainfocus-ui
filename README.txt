To view the project, select the build folder and open index.html in a browser.

To run the project locally, open a terminal in the project root directory. In the terminal, type npm install. Once completed, type npm run.